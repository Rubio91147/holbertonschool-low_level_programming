#include "main.h"
/**
* add - adds two integers and returns the result.
* description = add  of current result
* @x: is the argument in the funtion
* @y: is the argument in the funtion
* Return: result add un num
*/
int add(int x, int y)
{
int result;
result = x + y;
return (result);
}
