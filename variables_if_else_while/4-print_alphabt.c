#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
*main - Entry point
*
*Return: Always 0 (success)
*/
int main(void)
{
char alfabeto = 'a';
for (alfabeto = 'a'; alfabeto <= 'z'; alfabeto++)
{
  if(lower!= 'e' && lower!='q'){
        putchar(lower);
    }
}
putchar('\n');
return (0);
}
