#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - function that prints a string
 * description - prints string
 * @s: arguments the function
 */
void _puts_recursion(char *s)
{
	printf("%s\n", s);
}
